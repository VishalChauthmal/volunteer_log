<% provide(:title, @title) %>
<h1>All Attendees</h1>

<ul class="users">
	<% @users.each do |user| %>
		<li>
			<%= image_tag("atmalogo.jpg", alt: "Profile Photo", class: "pic") %>
			<%= link_to "#{user.name}", user %>
		</li>
	<% end %>
</ul>
