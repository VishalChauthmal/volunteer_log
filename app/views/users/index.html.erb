<%= provide(:title, 'All Users') %>
<h1>All Users</h1>

<%#= will_paginate %>

<ul class="users">
	<% @users.each do |user| %>
		<li>
			<%= image_tag("atmalogo.jpg", alt: "Profile Photo", class: "pic") %>
			<%= link_to "#{user.name}", user %>
		</li>
	<% end %>
</ul>

<%#= will_paginate %>